# Строгий rolling update: до (replicas + 1) подов на время выката (maxSurge: 1, maxUnavailable: 0).
# Нужен запас CPU/RAM на нодах; иначе новый под зависнет в Pending → Progress deadline exceeded.
rollingUpdate:
  maxSurge: 1
  maxUnavailable: 0
