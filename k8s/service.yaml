apiVersion: v1
kind: Service
metadata:
  name: bulletin-app
  namespace: bulletin
spec:
  type: LoadBalancer
  selector:
    app: bulletin-app
  ports:
    - name: http
      port: 80
      targetPort: 8080

